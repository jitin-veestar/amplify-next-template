// import type { Schema } from '../resource'

// export const handler: Schema['getUser']['functionHandler'] = async (event, context) => {
   
//     return {
//         userId: event.arguments?.userId ?? null,
//         name: event.arguments?.name ?? null,
//         facilityEmail: event.arguments?.facilityEmail ?? null,
//         city: event.arguments?.city ?? null,
//         country: event.arguments?.country ?? null,
//         facilityName: event.arguments?.facilityName ?? null,
//         acceptHippa: event.arguments?.acceptHippa ?? null,
//         updatedAt: new Date().toISOString(), // Ensure all required fields are included
//       };
//   };